# Sample { #sim_2024.Sample }

`Sample()`

custom type to store an array of float xor int values and some functionalities related

Sample has to porpuses. First, store a potentialy huge amount of numbers (either float or int)